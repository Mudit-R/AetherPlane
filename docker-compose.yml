version: '3.8'

services:
  aetherplane-engine:
    build: .
    container_name: aetherplane_data_plane
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - LINK_RATE_MBPS=100.0
      - LOG_LEVEL=INFO
